# Array Type

## ArrayType

The `ArrayType` can be used to work with plain arrays that are JSON encoded
manually.

::: tip
See the [Tools](https://github.com/dereuromark/cakephp-tools) plugin and its
`Jsonable` behavior for a higher-level approach.
:::
